Full Stack Weather App

we take a React + Node.js weather app from localhost to a live production website. 

We’ll deploy: 

Node.js Backend → Render 

React Frontend → Vercel 

We’ll also configure environment variables, fix CORS, connect the frontend to the backend, and test the final live application.
